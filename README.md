# auto_driving_vehicle

## Gazebo Sim

Run the world with the helper script:

```bash
./scripts/run_sim.sh
```

Or run directly from the command line:

```bash
GZ_SIM_RESOURCE_PATH=$PWD/sim/gazebo/models gz sim -r sim/gazebo/worlds/car_world.sdf
```
